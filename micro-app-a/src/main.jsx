import React from 'react'
import ReactDOM from 'react-dom/client'
import MicroComponentA from "./MicroComponentA";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MicroComponentA />
  </React.StrictMode>
);
