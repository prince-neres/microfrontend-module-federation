import React from 'react'
import ReactDOM from 'react-dom/client'
import MicroComponentC from "./MicroComponentC";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MicroComponentC />
  </React.StrictMode>
);
